[tool.poetry]
name = "corto-webapp"
version = "0.1.0"
description = "Web interface for Corto"
authors = ["gm2211 <gmecocci@outlook.com>"]
package-mode = false

[tool.poetry.dependencies]
python = "3.11.*"
flask = "^3.0.2"

[build-system]
requires = ["poetry-core>=1.9.0"]
build-backend = "poetry.core.masonry.api" 